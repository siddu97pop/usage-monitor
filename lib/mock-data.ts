// Mock data removed — production only.
// All data comes from real sources: SQLite (Codex), OAuth API (Claude), Supabase (Vercel fallback).
import type { ProviderStats } from '@/types/usage';
export const MOCK_DATA: ProviderStats[] = [];
