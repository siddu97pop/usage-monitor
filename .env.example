# ─── Database ────────────────────────────────────────────────────────────────
# Vercel Postgres (Neon) — copy the pooled connection string from the Vercel dashboard
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# ─── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ─── Future integrations (not used in v1) ────────────────────────────────────
# Ollama Pro API — add when available
# OLLAMA_API_KEY=""
# OLLAMA_BASE_URL="https://ollama.com"

# Claude subscription usage — subscription-based, not Admin API
# Future: browser-assisted session capture or manual import
# CLAUDE_SESSION_TOKEN=""

# Codex / ChatGPT subscription usage — subscription-based, not API billing
# Future: Compliance API token (cloud Codex usage only)
# OPENAI_COMPLIANCE_API_KEY=""
